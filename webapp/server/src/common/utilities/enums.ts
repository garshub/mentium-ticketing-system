export enum UserRole {
  AGENT = 'AGENT',
  ADMIN = 'ADMIN',
  CUSTOMER = 'CUSTOMER',
}

export enum TicketStatus {
  NEW = 'NEW',
  OPEN = 'OPEN',
  PENDING = 'PENDING',
  CLOSED = 'CLOSED',
}

export enum TicketPriority {
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH',
}
